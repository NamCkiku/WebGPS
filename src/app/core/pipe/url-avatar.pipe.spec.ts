import { UrlAvatarPipe } from './url-avatar.pipe';

describe('UrlAvatarPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlAvatarPipe();
    expect(pipe).toBeTruthy();
  });
});
